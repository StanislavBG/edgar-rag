run = "python -m uvicorn src.server:app --host 0.0.0.0 --port 3000"
entrypoint = "src/server.py"

[nix]
channel = "stable-24_05"

[env]
PORT = "3000"

[deployment]
run = ["sh", "-c", "pip install -e . && python -m uvicorn src.server:app --host 0.0.0.0 --port 3000"]
